{
  "name": "chibatoru-websocket-server",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket room server for Chibatoru online battles.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test server.test.js && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
