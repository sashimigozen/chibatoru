# チバトル開発ガイドライン

## 1. プロジェクト概要

このプロジェクトはデジタルカードゲーム「チバトル」の開発です。

チバトルは学校をテーマにした対戦型カードゲームです。

プレイヤーはカードを出席させて盤面を作り、相手プレイヤーの体力を0にすることで勝利します。

---

# 2. チバトル基本仕様

## 盤面

### 席マス

学生カードを配置する場所。

### 教卓マス

教師カードを配置する場所。

### 環境マス

環境カードを配置する場所。

環境カードは基本的に双方のプレイヤーへ影響します。

### 遅刻ゾーン

出席まで待機するカードを管理する場所。

カードごとに必要なターン数があります。

### 校外エリア

破壊されたカード、使用済みカードなどを管理する場所。

---

# 3. 仕様確認ルール

仕様を確認する場合、以下の情報を優先する。

## 仕様確認の優先順位

1. 現在の実装コード
2. ルール・用語管理台帳
3. カード管理台帳
4. GitHub上の最新状態・履歴
5. 過去の会話や推測

仕様変更やカード修正を行う場合、必要に応じて関連資料を確認する。

資料と実装が異なる場合は、勝手に判断せず、現在の目的と影響範囲を確認する。

---

# 4. GitHub利用ルール

GitHub上の情報を確認してよい。

GitHub履歴の確認は、現在の実装だけでは変更理由や意図を判断できない場合に限る。

通常の修正では、まず現在の実装コードと関連資料を確認し、必要性がなければ過去コミットや修正履歴まで調査しない。

確認対象：

- 過去コミット
- 修正履歴
- 実装済み機能
- 関連ファイル
- 変更理由

ただし以下は禁止。

- 不要なファイル変更
- 過去の安定した実装の削除
- 理由のない大規模変更

変更前後は必ずdiffを確認する。

---

# 5. カード実装ルール

カードを修正・追加する場合、以下を確認する。

1. カードID
2. カード定義
3. デッキ登録・カテゴリー
4. カードテキスト
5. 使用可能判定
6. 対象選択
7. 効果処理
8. 出席可能場所
9. AI評価
10. オンライン同期
11. カードテスト
12. 自動テスト
13. 更新履歴・バージョン

変更内容に応じて、必要な箇所だけを確認・変更する。

既存カードのIDは、現在の実装を確認して使用し、カード名から推測して新しいIDを作らない。

新カードで本当に新規IDが必要な場合だけ、新しいIDを決める。

単純な数値変更では、関係のない効果処理、AI、オンライン処理、UIまで変更範囲を広げない。

---

# 6. カード効果修正ルール

効果変更時は以下を確認する。

- カードテキスト
- 使用可能判定
- 発動条件
- 対象選択
- 効果処理
- 状態変更
- 破壊処理
- 手札・デッキ操作
- 場所制限

カードテキスト、使用可能判定、実際の効果処理は別々に実装されているため、変更内容に応じて整合性を確認する。

対象を選択する効果の場合、

1. 選択UI
2. 選択結果
3. 効果実行

の全てを確認する。

---

# 7. UI変更ルール

UI変更時はCSSだけを変更しない。

必ず以下の順番で確認する。

1. 状態を作る処理
2. classを付与する処理
3. CSS

例：

出席不可マスの場合：

状態判定

↓

class付与

↓

CSS表示

の流れを確認する。

CSS変更時は以下を確認する。

- 後から読み込まれるCSS
- CSSの優先順位
- !importantによる上書き
- 同じclassへの別定義

---

# 8. index.html変更ルール

index.htmlは大規模ファイルのため、全体を書き換えない。

変更時：

1. grepなどで対象箇所を探す
2. 周辺コードを確認する
3. 必要最低限だけ変更する
4. diff確認する

不要なリファクタリングは禁止。

動いている機能を壊さないことを優先する。

---

# 9. 開発方針

このプロジェクトでは、

「コードを綺麗にすること」

よりも、

「現在動いているゲームを壊さず改善すること」

を優先する。

既存実装には理由がある可能性があるため、削除・変更前に確認する。

---

# 10. テストルール

機能変更後は可能な限り確認する。

カード効果変更の場合：

- 条件確認
- 発動確認
- 盤面状態確認

を行う。

必要に応じてPlaywrightなどのテストを使用する。

---

# 11. Git運用

既存のworking tree変更は保持する。

ユーザーの指示なしに、git reset、git restore、git checkout --、stashなどで既存変更を消さない。

今回の作業と無関係な変更を巻き戻さない。

変更後は必ず確認する。

確認順：

git status

↓

git diff

↓

問題なければcommit

↓

push


コミットメッセージは変更内容が分かるものにする。

良い例：

- Fix Word Increaser text
- Fix card effect bug
- Improve blocked seat display

悪い例：

- update
- fix
- test

---

# 12. AI開発効率化ルール

長時間タスクではコンテキストを整理する。

保持するもの：

- 現在の目的
- 決定事項
- 重要な仕様
- 制約
- 現在の進捗
- 未解決問題
- 次の作業

削除するもの：

- 古い失敗ログ
- 不要な試行
- 重複説明
- 現在の作業と無関係な履歴

---

# 13. Prompt Cacheを意識した作業

固定情報を優先して保持する。

固定情報：

- プロジェクトルール
- ゲーム仕様
- 開発規約
- コーディング方針

変化する情報：

- 今回の修正内容
- 一時的なバグ情報

は後から追加する。

同じ内容を毎回再説明しない。

---

# 14. ツール利用方針

必要なツールだけ使用する。

不要な情報やツールを大量に読み込まない。

関連箇所が特定できたら、必要のない探索を続けない。

巨大ファイルは、grepやrgなどで対象箇所を絞ってから周辺だけ確認する。

小さな変更では、変更箇所に対応した必要最小限のテストを優先する。

変更の影響範囲が広い場合だけ、広範囲のテストや追加調査を行う。

基本的に使用するもの：

- ローカルファイル確認
- Git
- テスト環境

必要な場合のみ：

- GitHub履歴確認
- その他外部ツール

を使用する。

---

# 15. 作業開始手順

作業開始時：

1. 現在状態を確認する
2. 関連ファイルと対象箇所を確認する
3. 必要な場合だけ仕様資料や履歴を確認する
4. 原因または変更対象を特定する
5. 必要最小限の修正を行う
6. 変更内容に応じたテストを行う
7. diffを確認する

---

# 16. 完了条件

作業完了とは以下を満たす状態。

- 要望通り動作する
- 既存機能を壊していない
- 変更理由を説明できる
- diff確認済み
- 必要なテスト済み
- 不要な変更がない
- 今回の要望以外へ変更範囲を広げていない
- 既存のworking tree変更を壊していない

---

# 17. 最重要方針

チバトルは長期開発プロジェクトである。

将来の拡張を推測して変更範囲を広げるより、現在動いているゲームを壊さず、今回の要望を必要最小限の変更で実現することを優先する。

判断に迷った場合：

1. 現在の実装と既存仕様を確認する
2. 影響範囲を小さくする
3. 推測で仕様を補わない
4. 必要な場合だけ関連資料や履歴を確認する
5. それでも判断できない場合だけ質問する
6. 今回の要望に含まれない「ついでの改善」は行わない
